-- Quantidade de pilotos registrados
SELECT COUNT(*) qtd_pilotos
    FROM driver;


-- Quantidade de escuderias registradas
SELECT COUNT(*) qtd_escuderias
    FROM constructors;


-- Quantidade de corridas registradas
SELECT COUNT(*) qtd_corridas
    FROM races;


-- Quantidade de temporadas registradas
SELECT COUNT(*) qtd_temporadas
    FROM seasons;
